# Deferred Work

## Deferred from: code review of 1-1-project-foundation-and-reproducible-setup (2026-07-10)

- `configure_logging()` runs as an import side effect of `app.main` ([backend/app/main.py:24](../../backend/app/main.py)), replacing the root logger's handlers for any process that imports the module — pytest included (`tests/test_health.py` imports `app.main` at collection). Low consequence today; revisit if `caplog`-based tests arrive or if a host application ever embeds the app. Candidate fix: move configuration behind an app factory or uvicorn `log_config`.
- A top-level pip package literally named `seed` is installed into site-packages (`include = ["app*", "seed*"]` in [backend/pyproject.toml:54](../../backend/pyproject.toml)), and the api image keeps a duplicate source copy at `/srv` on `sys.path` alongside the installed one. `seed` is a collision-prone module name and two copies of a package on `sys.path` is an ambiguity waiting for a symptom — but the story's prescribed source tree fixes `backend/seed/`, so renaming is a spec change, not a patch.

## Deferred from: code review of 1-5-manage-departments (2026-07-11)

- **Enveloped server-side validation for department `name` (and free-text writes generally).** Empty/whitespace names are currently accepted (`POST/PATCH /departments {"name": ""}`), violating the server-as-guard invariant (NFR-03). Blocked on an architecture decision: how validation failures are enveloped in the NFR-17 `{code,message,details}` contract (new vocabulary code + status mapping), since a raw pydantic 422 bypasses it. This is the first free-text write; the convention set here will apply to every later validated field. [backend/app/api/v1/departments.py, backend/app/services/departments.py]
- **Pagination controls for the Departments UI.** The backend pagination convention is fully built (bound 50/100), but the frontend requests only the first page and renders no next/prev controls, so departments past the first page are unmanageable via the UI. Deferred until a real >50-department need appears. [frontend/src/api/departments.ts, frontend/src/features/departments/DepartmentsPage.tsx]

## Deferred from: code review of 1-6-manage-employees-and-reporting-lines (2026-07-11)

- **Pagination controls for the Employees UI, and the Manager `<select>` cap.** `useEmployees()` fetches only the first page (default 50), and both the list and the Manager dropdowns render from that page. With >50 employees, rows 51+ are invisible/unmanageable and a manager beyond row 50 cannot be assigned — a concrete AC13 gap at scale. Same shape as the deferred Departments pagination gap; a shared pagination-UI treatment likely resolves both. [frontend/src/features/employees/EmployeesPage.tsx, frontend/src/api/employees.ts]
- **AD-22 no-orphan invariant is one-directional and non-atomic.** Nothing prevents naming a *deactivated* Employee as a new manager (Open Q3 — allowed by design, flagged as a possible future guard), and the `count_active_direct_reports` deactivation/demotion guard has no DB backstop and no row lock, so a concurrent `POST /employees {manager_id: M}` and `POST /employees/M/deactivate` can both commit and orphan an active report. The orphaning consequence (auto-approval of the report's requests) is vacuous until Epic 2 creates `leave_request`; revisit when that guard becomes executable. [backend/app/services/employee.py]
- **Email uniqueness is case/whitespace-sensitive; no server-side email normalization or format validation.** `get_by_email` is an exact match, `email` is a bare `str` (not `EmailStr`), and the service never trims/lowercases, so `Foo@x.com` and `foo@x.com` are two distinct loginnable accounts and the "one address, one account" intent is not enforced. Pre-existing (the login path shares `get_by_email`); folds into the same enveloped-validation contract deferred for department/employee free-text (Open Q2). [backend/app/services/employee.py, backend/app/repositories/employee.py]
- **A >72-byte initial password 500s.** `create_employee` calls `security.hash_password`, which raises `ValueError` on a password over bcrypt's 72-byte limit; it is not caught, so it surfaces as a 500 rather than an enveloped 4xx. Explicitly sanctioned by Trap 5 / Open Q2 pending the validation-error contract. [backend/app/services/employee.py:162]
- ~~**Unbounded `page` query param.**~~ **CLOSED by Story 3.1** (Open Decision #5, adopting this entry by name): `MAX_PAGE = 1_000_000` downward clamp in `PageParams` — never a 422, `offset` stays inside bigint. Struck by Story 3.5 (Task 10; 3.1 flagged it stale, 3.4 authorized the strike). [backend/app/api/v1/pagination.py:43]

## Deferred from: code review of 1-7-scope-authority-to-the-reporting-relationship (2026-07-11)

- **The SM-3 matrix registers a scope per endpoint but never validates the registered value against api-contracts §4.** `test_each_registered_operation_grants_at_least_one_scope` only asserts the value is a non-empty set of `Scope` members; registering the *wrong* scope (e.g. `{Scope.REPORTS}` on an Admin-only endpoint) would pass unnoticed. For Epic 1 every value is correctly `{Scope.ALL}`, so the gap is latent. No machine-readable source of §4 grants exists in code to cross-check against — resolving this needs a decision on whether §4 gets a codified representation. Epic 2, which introduces the first multi-scope endpoints (`GET /leave-requests/{id}` → self/reports/all), inherits the exposure. [backend/tests/test_scope_matrix.py:163]
- **`include_in_schema=False` identifier endpoints escape the AC4 completeness gate.** Route enumeration reads `app.openapi()["paths"]`; any future identifier endpoint declared hidden from the schema is routed and authorization-guarded but invisible to the completeness assertion, silently escaping "every identifier endpoint declares a scope." No such endpoint exists in Epic 1 (chosen because `app.routes` does not expose nested `APIRoute`s under the pinned FastAPI 0.139.0). Revisit when Epic 2 adds the Leave-Request identifier surface — if any endpoint there is hidden, the harness under-covers it. [backend/tests/test_scope_matrix.py:93]

## Deferred from: code review of 2-1-leave-types-as-configuration (2026-07-13)

- **Enveloped server-side content validation for the Leave Type write body.** `LeaveTypeWriteRequest` validates only types. Gaps: empty/whitespace `code`/`name` accepted (`code` is the meaning-bearing UNIQUE key); `annual_entitlement ≤ 0` accepted (a bad day-count that will corrupt Epic 2 balance/proration math); `annual_entitlement`/`carry_forward_cap` > INT32 max → `DataError` → uncaught raw 500; `carry_forward_cap` set while `carries_forward=false` (ERD §6 calls it meaningless — frontend guards it, backend/DB do not); `code` uniqueness is case/whitespace-sensitive so `el` and `EL` coexist. Same blocker as the 1.5/1.6 free-text deferrals: pending the architecture decision on how validation failures are enveloped in the NFR-17 `{code,message,details}` contract (a raw Pydantic 422 bypasses it). The convention set there applies to every validated write field. [backend/app/api/v1/leave_types.py:45-53, backend/app/services/leave_types.py]
- **Pagination controls for the Leave Types UI.** `useLeaveTypes()` fetches page 1 only (page_size 50) and `LeaveTypesPage` renders no next/prev controls, so a 51st+ leave type is invisible in the UI. Same shape as the deferred Departments/Employees pagination gaps — a shared pagination-UI treatment resolves all three. Low urgency: leave types are inherently few (3 seeded). [frontend/src/api/leaveTypes.ts:54-59, frontend/src/features/leaveTypes/LeaveTypesPage.tsx]

## Deferred from: code review of 2-2-the-company-holiday-calendar (2026-07-13)

- **Enveloped server-side body validation for the holiday write body.** Empty/whitespace `name` is accepted and a malformed `holiday_date` returns a raw pydantic 422 outside the `{code,message,details}` envelope (its DATE *type* is still enforced). Same blocker as the 1-5/2-1 name-validation deferral — pends the NFR-17 enveloping decision; `departments` and `leave_types` share the identical gap. Already declared in this story's Dev Agent Record. [backend/app/api/v1/holidays.py, backend/app/services/holidays.py]
- **Pagination controls for the Holidays UI.** `useHolidays()` fetches only the first page (default 50) and the screen renders no next/prev controls, so holidays past page 1 are invisible and undeletable via the UI. Same shape as the deferred Departments/Employees pagination gaps; a shared pagination-UI treatment resolves all of them. [frontend/src/api/holidays.ts, frontend/src/features/holidays/HolidaysPage.tsx]
- **Concurrent same-id delete returns 500 instead of 404.** Two admins issuing `DELETE /holidays/{id}` for the same row concurrently: both pass the load-or-404, the losing commit deletes 0 rows and SQLAlchemy raises `StaleDataError` (uncaught → 500) rather than the intended `404 RESOURCE_NOT_FOUND`. Narrow race; the same delete shape is used by departments/leave_types, so this is a codebase-wide decision. [backend/app/services/holidays.py:102]
- **Test coverage: the genuine-TOCTOU `IntegrityError` re-check branch is never exercised.** `test_a_duplicate_holiday_date_is_409` only hits the sequential pre-check; the `except IntegrityError` rollback→re-check→typed-409 branch (the concurrent-insert path) has no test, so a regression there ships green. [backend/tests/integration/test_holidays.py]
- **Test robustness: the page-size clamp test seeds fixed dates (`2200-01-01…`) and cleans up only in `finally`.** A process killed mid-body leaves rows that collide with `UNIQUE (holiday_date)` and fail every subsequent run until the table is purged manually. [backend/tests/integration/test_holidays.py]
- **Test robustness: `_unique_date` can collide.** It picks one date from a ~9,999-day window via `uuid4().int % 9999`; under `pytest-xdist` or overlapping runs against a shared DB, two tests can pick the same date and hit an unexpected 409. [backend/tests/integration/test_holidays.py]

## Deferred from: code review of 2-4-leave-balances-three-quantities-one-derived (2026-07-13)

- **Year-rollover cliff — reads go empty and mutations 500 once the calendar year turns.** Balances are materialized only for `date.today().year` at create time; both reads (`balance_reads._current_leave_year`) and mutations hardcode the current year. An Employee whose rows were materialized in a prior year has no current-year row, so `GET /balances` returns empty and any `reserve`/`consume` hits `_lock` → `LookupError` → 500. The rollover that materializes next-year rows is assigned to Story 2.10; nothing in 2.4 degrades gracefully in the interim. [backend/app/services/balance_reads.py, backend/app/services/balances.py]
- **`set_accrual` has no `available ≥ 0` guard on its DO-UPDATE branch.** On a recalculation that lowers `accrued` below existing `consumed + reserved`, the `accrued - consumed - reserved >= 0` CHECK fires as a raw 500. `adjust_reserved`/`adjust_consumed` guard this; `set_accrual` does not. Not reachable in Story 2.4 (materialization is fresh inserts, `reserved=consumed=0`); the DO-UPDATE lowering path and its per-pair refusal / `admin_review` handling belong to Story 2.11/2.12 recalculation. [backend/app/services/balances.py:267]
- **Negative `annual_entitlement` reaches proration and fires a CHECK 500.** `LeaveTypeWriteRequest.annual_entitlement: int` has no `Field(ge=0)` constraint, so a negative entitlement can be created; materialization then produces a negative `accrued` and the `available ≥ 0` CHECK fires (500) on leave-type creation. Root cause is the missing validation in Story 2.1's leave-type schema; Story 2.4's materialization newly surfaces it as a 500. Fix belongs with leave-type entitlement validation. [backend/app/api/v1/leave_types.py:50]
- **No index leading with `leave_type_id` on `leave_balance`.** The composite `UNIQUE (employee_id, leave_type_id, leave_year)` serves employee-leading lookups (`lock_balance`, `list_balances`) but a `leave_type_id`-leading access (FK enforcement / a future leave-type delete) sequential-scans `leave_balance`. Negligible at Epic-2 scale; the migration consciously declared no second index for the employee-leading path only. [backend/alembic/versions/0005_leave_balance.py]
- **Concurrent Employee-create / Leave-Type-create leaves a permanently-missing balance row.** Under READ COMMITTED, `create_employee` (loops `all_leave_types`) and `create_leave_type` (loops `all_employees`) running concurrently neither see the other's uncommitted row, so the new `(employee, type, year)` balance is never materialized; the next `reserve`/`consume` for that pair hits `_lock` → `LookupError` → 500. Accepted as a known limitation (code review 2026-07-13, option 1): admin creates are rare and effectively serial, and the outcome is recoverable by re-materializing. Revisit if concurrent admin provisioning becomes real; candidate fixes are lazy materialize-on-demand in `_lock`, an advisory lock, or SERIALIZABLE + retry. [backend/app/services/employee.py:214, backend/app/services/leave_types.py:105]

## Deferred from: code review of story-2.6 (2026-07-13)

- **No duplicate / overlapping-request guard on submit.** The same `(leave_type_id, start, end)` can be submitted repeatedly — double-click, two tabs, or a direct API call — each creating a distinct PENDING `leave_request` that reserves the days again (bounded only by Available). `submit_leave_request` inserts unconditionally and no overlap/idempotency check exists; the frontend keeps the form and re-enables Submit after success. No Story 2.6 AC covers overlap detection. Candidate fixes: a server-side overlap query before insert, an idempotency key, and/or clearing/disabling the frontend form on success. [backend/app/services/leave_requests.py:288, frontend/src/features/leave/RequestPreviewPanel.tsx:91]
- **Deactivation pending-request guard is a TOCTOU race.** `deactivate_employee` reads `count_pending_for_employee` without locking the `employee` row, while `submit_leave_request` locks only the balance row — the two transactions share no lock, so a submit interleaved with a deactivation can leave an inactive employee holding a live PENDING request (the state AC7/AD-22 exists to prevent). Narrow interleaving; admin deactivation is effectively serial; mirrors the pre-existing `EMPLOYEE_HAS_DIRECT_REPORTS` guard's shape. Fix would lock the employee row (or overlap the locks) across the guard + both writers. [backend/app/services/employee.py:374]
- **A deactivated employee holding an unexpired token can still submit and reserve balance.** `get_current_employee` → `resolve_actor` deliberately does not re-check `is_active` (AD-14); `is_active` is verified only at login. The new `POST /leave-requests` write endpoint adds no gate of its own, so a just-deactivated user can mutate balance until their token expires. Pre-existing auth design, not introduced by 2.6; flagged because this is the first balance-mutating consequence. Revisit if/when token revocation or an `is_active` gate on write endpoints is introduced. [backend/app/services/leave_requests.py:202, backend/app/services/auth.py:122]

## Deferred from: code review of story-2.7 (2026-07-13)

- **`consume_reserved`/`release_reserved` `ValueError` → raw 500 if `reserved` is moved out of band below `leave_days`.** The lock-order argument (guarded UPDATE before balance mutate) only covers concurrent *lifecycle transitions*, which the row lock serializes into a clean 409. It does not cover the `reserved` counter being lowered out of band (e.g. an Admin `adjust_reserved`) while the request stays PENDING: then `approve` passes the `WHERE status = PENDING` guard, `consume_reserved(days)` finds `days > reserved` and raises `ValueError` (not a `DomainError`) → raw 500. Latent — reachable only once an out-of-band reserved-adjust endpoint ships. Candidate fix: wrap the `mutate` call to translate the balance `ValueError` into a domain refusal. [backend/app/services/leave_requests.py, backend/app/services/balances.py:141]
- **The clean-409 (AD-4/AC2) guarantee silently depends on READ COMMITTED isolation.** The lost-race-becomes-409 behaviour relies on the blocked second `UPDATE` re-evaluating `WHERE status = :from` against the newly committed row after the first transaction releases the lock — which only happens under READ COMMITTED (Postgres default). Under REPEATABLE READ / SERIALIZABLE the second `UPDATE` would raise a 40001 serialization failure → uncaught 500, breaking AC2. Nothing pins the isolation level, so a future `isolation_level=` change would silently convert every lost race from 409 to 500. Candidate fix: assert/pin the engine isolation level, or document the dependency at the engine config. [backend/app/repositories/leave_request.py transition_status]
- ~~**Unbounded `page` overflows the SQL bigint `OFFSET` → raw 500.**~~ **CLOSED by Story 3.1** (Open Decision #5, adopting this entry by name): `MAX_PAGE = 1_000_000` downward clamp in `PageParams`, pinned by a DB-free `page=10**18`-stays-inside-bigint test. Struck by Story 3.5 (Task 10; 3.1 flagged it stale, 3.4 authorized the strike). [backend/app/api/v1/pagination.py:43]

## Deferred from: code review of 2-8-cancel-approved-leave-through-a-cancellation-request (2026-07-13)

- **List pagination is page-1-only across all three 2.8 panels** (Admin cancellation queue, applicant approved-leave list, applicant CR list). DEFAULT_PAGE_SIZE=50, no pager. Pre-existing app-wide pattern (ManagerQueuePanel, `useLeaveRequests`). Elevated concern: the Admin cancellation queue is the *only* route to discover a PENDING CR, so items beyond 50 are undecidable until earlier ones clear.
- **Test coverage gaps for 2.8**: the `end_date == today` in-progress boundary of `is_wholly_past` (spec-sanctioned as cancellable per AC3 note, but unverified), and the Manager/Admin API-only self-cancel raise path (scope SELF, role-agnostic per Open Decision #6) — both currently untested.

## Deferred from: implementation of 2-11-a-holiday-change-recalculates-and-may-be-refused (2026-07-14)

- ~~**`reserve` and `consume_direct` never re-derive `carried_forward` — so `carried_forward(Y+1)` can be STALE-HIGH.**~~ **CLOSED by Story 3.4 (Task 11)**: `submit_leave_request` now calls `rollover.recompute_carry_forward` after `reserve`/`consume_direct` — but **NOT as the "one more call" this entry prescribed**, which 3.4's Landmine 15 declared UNSAFE as written (an unguarded call would have shipped a NEW raw 500 on the submit path: submit LOWERS `carried_forward(Y+1)`, and a spent `Y+1` trips `set_accrual`'s `ValueError`). The recompute is FORWARD-CHECKED: it projects with `domain/recalculation.project_forward` before its first write, and on refusal writes NO balance, appends one `admin_review_flag` (`CAUSE_SUBMISSION_RECALCULATION`, deduped per pair/year/cause since the 2026-07-15 code review), and the submission still commits. Pinned by `test_ad6_a_submission_that_cannot_reconcile_is_flagged_and_still_commits`. Struck by the 2026-07-15 code review (3.4 closed it and did not strike it). [backend/app/services/leave_requests.py submit_leave_request, backend/app/services/rollover.py recompute_carry_forward]
- **A request recalculated to ZERO working days is refused-and-flagged, and stays Pending/Approved forever.** Story 2.11 Open Decision #3, the one genuinely under-determined point in the story: an ADD can price a one-working-day request down to zero, which `CHECK (leave_days > 0)` forbids writing. The pair is refused and flagged (conservative, reversible, and it never fires the CHECK). But nothing then *resolves* that request: it keeps its stale `leave_days` and its reserved/consumed days, and no requirement grants a transition that would release them. Auto-cancelling it was rejected because it invents a state transition no requirement grants and would need an `audit_entry` row (breaking SM-4's exact-count premise). The Admin sees the flag and can act — but the ACTION they would take (cancel the request) has no first-class support. Revisit if this proves common. [backend/app/services/recalculation.py]
- **`admin_review_flag` has no index, and it is the table most likely to be read whole.** ERD §4.4 names no index for it and none was created. The read is `ORDER BY occurred_at DESC, id DESC` with `LIMIT/OFFSET`, so a growing register sequential-scans and sorts. Negligible at Epic-2 scale; mirrors the same call `audit_entry` made (no `occurred_at` index, flagged in 2.9). [backend/alembic/versions/0010_admin_review_flag.py]
- **The refusal register's list is page-1-only, like every other list in the app.** `ReviewFlagsPanel` renders `items` with no pager. The known, accepted, app-wide deferral (2.8, 2.9). Elevated slightly here for the same reason it was for the Admin cancellation queue: this screen is the ONLY route to discover a refused recalculation, so refusals beyond the first page are invisible. [frontend/src/features/reviewFlags/ReviewFlagsPanel.tsx]

## Deferred from: implementation of 2-12-change-leave-policy-with-an-explicit-disposition (2026-07-14)

- ~~**⚠️ A pair REFUSED during a `carry_forward_cap` decrease is left holding a carry-forward the new policy forbids — and the next unrelated `recompute_carry_forward` on it raises a bare `ValueError` → RAW 500 on an innocent third party.**~~ **CLOSED by Story 3.4 (Task 11) and the 2026-07-15 code review**, in the skip-and-flag disposition this entry named: `recompute_carry_forward` is now forward-checked, so the innocent third party's reject commits cleanly and the unreconcilable pair is flagged (`CAUSE_TRANSITION_RECALCULATION`), never a 500 — the canary `test_a_refused_pair_still_carries_a_stale_cap_into_an_unrelated_reject` failed exactly as this entry predicted and was REPLACED by `test_a_refused_pair_with_a_stale_cap_is_flagged_not_500`. The entry's last clause — "the next `run_rollover` aborts its whole batch on the same row" — outlived Task 11 (the batch loop writes through `set_accrual` directly and never calls the guarded recompute) and was closed separately by the 2026-07-15 code review: `run_rollover` now forward-checks each pair, refuses per pair (`CAUSE_ROLLOVER_RECALCULATION`, no balance write, batch continues), pinned by `test_a_refused_pair_does_not_abort_the_rollover_batch`. The refused pair itself still holds the stale carry-forward indefinitely — that is AD-19's refusal posture, surfaced to an Admin via the flag register, and resolving WHAT the Admin then does remains open product work. [backend/app/services/rollover.py recompute_carry_forward + run_rollover]
- ~~**Story 2.11's Open Decision #8 (`reserve`/`consume_direct` never re-derive `carried_forward`) is STILL LIVE, and Epic 2 is now over.**~~ **CLOSED by Story 3.4 (Task 11)** — Epic 3 did pick it up: the submit-side recompute is wired into `submit_leave_request` with `leave_year=start.year`, forward-checked (NOT the naive one-liner this entry restated — see the struck 2.11 entry above for why that formulation was unsafe as written). Struck by the 2026-07-15 code review (3.4 closed it and did not strike it). [backend/app/services/leave_requests.py submit_leave_request]
- **The policy-change log's list is page-1-only, like every other list in the app.** `PolicyChangesPanel` renders `items` with no pager. The known, accepted, app-wide deferral (2.8, 2.9, 2.11). [frontend/src/features/policyChanges/PolicyChangesPanel.tsx]
- **`policy_change` has no index, and it is read whole, newest first.** ERD names none and none was created. `ORDER BY occurred_at DESC, id DESC` with `LIMIT/OFFSET` sequential-scans and sorts. Negligible at Epic-2 scale; the same call `audit_entry` (2.9) and `admin_review_flag` (2.11) both made. [backend/alembic/versions/0011_policy_change.py]
- **Non-contiguous materialized years silently truncate the policy recalculation (Open Decision #9, NOT implemented).** `_materialized_years` walks upward from the pair's lowest year and stops at the FIRST GAP. A skipped rollover (`--year 2027` without `--year 2026`) leaves a hole, and every year **above** the hole keeps its old proration and old `entitlement_basis` forever, with no flag — a wrong balance that will be believed. The story recommended detecting it (the sweep already knows each pair's `MAX(leave_year)`; refuse the pair if it exceeds the walk's last year) and it was **not** built: it is operator error, the rest of the system already assumes contiguity everywhere (`recompute_carry_forward` and `run_rollover` both stop at the first gap identically), and adding a refusal path here without adding it there would be a half-guarantee. Logged by name as the story instructed. [backend/app/services/recalculation.py _materialized_years]

## Deferred from: code review of stories 3-1, 3-2, 3-3, 3-4, 3-5 (2026-07-15)

- **`DecisionCalendar` per-queue-row fan-out.** One inline calendar per pending row means 1+N GETs on mount (51 on a full default page) and `invalidateAfterDecision`'s whole-`CALENDAR`-prefix invalidation refetches every window after each approve/reject. Spec-conformant (3.3 AC2 orders the calendar inline per row); revisit only with a measured problem. [frontend/src/features/leave/ManagerQueuePanel.tsx, frontend/src/api/leaveRequests.ts]
- **Frontend status literals are re-hardcoded off any vocabulary guard.** `STATUS_OPTIONS = ['PENDING','APPROVED','REJECTED','CANCELLED']` in the history filter (and the calendar's fixed set) duplicate `domain/vocabulary.py`; the backend guard scans only `app/`, so a future status is silently absent from the filter. Acknowledged precedent — frontend literal copies are unguarded app-wide; extending the guard scan is its own task. [frontend/src/features/leave/MyLeaveHistoryPanel.tsx:36]
- **The forward-check `FOR UPDATE` walk runs on every submit.** `materialized_years` locks the pair's year and every later materialized year on each submission, even Jan–Nov when no `Y+1` row exists and the recompute is a guaranteed no-op; once `Y+1` is materialized, all writers of a pair serialize on the multi-year lock chain on the hottest write path. Settled Task 11 (3.4) design; revisit with measurement, not speculation. [backend/app/services/rollover.py materialized_years, backend/app/services/leave_requests.py submit_leave_request]
- **`REQUEST_SUBMITTED` can be addressed to a deactivated Manager.** The submit branch tests only `actor.manager_id is None`, not the manager's `is_active`; a report whose manager was deactivated without reassignment gets a PENDING request whose sole notification is addressed to someone who cannot sign in. Pre-existing reporting-line gap (deactivation does not reassign reports — the whole approval flow is broken for that report, not just the notification); fold into any future reassignment/deactivation guard work. [backend/app/services/leave_requests.py:417]

## Deferred from: code review of stories 4-1, 4-2 (2026-07-15)

- **Attach path concurrency: two unguarded races.** (a) Concurrent first-attach: both callers read `existing is None`, both INSERT, the loser's commit fires the `supporting_document_leave_request_id_key` UNIQUE backstop as a raw 500 and its already-written file becomes an orphan — contradicting the code's own "never surfaces" comment. (b) TOCTOU on the PENDING gate: attach reads PENDING → approve commits → attach commits, so "frozen" evidence lands on a just-decided request with no refusal (OD#2's intent under concurrency). Spec-compliant as written — Landmine 7 offered "row lock OR explicit SELECT" and the weaker option was taken; single-user-per-request in practice. Fix family: `with_for_update()` locate on the attach path. [backend/app/services/documents.py:250-283]
- **The "at most one byte over the limit is buffered" memory-bound claim is false.** `await request.form()` / FastAPI multipart parsing spools the ENTIRE part to a SpooledTemporaryFile before the capped `DOCUMENT_MAX_BYTES + 1` read. Deployed it's bounded by nginx's 6m; the dev server and TestClient path accept an arbitrarily large disk spool from any authenticated caller. Real fix is a streaming guard; at minimum the module docstring's guarantee should be restated as what it is (a response-code bound, not a memory bound). [backend/app/services/documents.py:20, backend/app/api/v1/leave_requests.py:400]
- **Uploads over 6 MB get nginx's raw HTML 413 outside the error envelope, and the 5 MB<x≤6 MB window is unpinned through the proxy.** A direct client posting 7 MB never sees `FILE_TOO_LARGE`; the frontend renders the synthesized no-envelope message. Documented tradeoff owned by the nginx.conf comment; nothing in any suite can see the proxy. [proxy/nginx.conf:36]
- **Hardcoded 2026 dates are a suite-wide time bomb.** `test_supporting_document.py` pins `date(2026, 8, 18)` etc., so from 2026-08-19 every submit-path test trips `PAST_DATE_RANGE` — but this is the established house pattern (11 integration files carry `date(202…)` literals; `test_leave_request_preview.py` alone has 11). Fix suite-wide in one pass (relative-to-today date factory), not per story. [backend/tests/integration/]
- **`original_filename` is accepted empty or arbitrarily long.** `part.filename or ""` is stored verbatim (column is `Text`): `""` yields `Content-Disposition: attachment; filename*=""` and the browser invents a download name; a multi-kilobyte name is echoed back as an oversized response header the proxy may reject. No requirement bounds it; pick a bound (e.g. 255, fallback name) when the contract next opens. [backend/app/api/v1/leave_requests.py:404, backend/app/api/v1/documents.py:98]
- **Frontend restates the document policy (5 MB cap + type allowlist) as literals.** `RequestPreviewPanel.tsx` duplicates `DOCUMENT_MAX_BYTES`/`DOCUMENT_CONTENT_TYPES` with nothing tying them to `domain/vocabulary.py`; raise the backend cap and the pre-check silently rejects legal files client-side. Same accepted class as the app-wide frontend status-literal copies (already logged 2026-07-15); fold into any future vocabulary-guard extension. [frontend/src/features/leave/RequestPreviewPanel.tsx:35]
- **A failed `path.write_bytes` leaves a partial file nothing unlinks, and the OSError escapes as a raw 500.** Disk-full mid-write on a 5 MB payload rolls the row back but the partial file lingers as an unreachable orphan — the accepted-orphan analysis (OD#4) covers only the crash-between-write-and-commit window, not this path. Envelope-on-500 is an app-wide gap (only DomainError has a handler), not this story's. [backend/app/services/documents.py:214, backend/app/services/documents.py:273]

## Deferred from: code review of logout feature (2026-07-15)

- source_spec: `_bmad-output/implementation-artifacts/spec-logout.md`
  summary: Logout in one browser tab does not sign out other open tabs of the same app.
  evidence: The token gate and the SESSION_EXPIRED path are both per-tab; nothing listens to the `storage` event, so a `localStorage` token removal in one tab leaves other tabs' in-memory `memoryToken` intact and fully signed in until the JWT's `exp` (it is non-revocable by design, NFR-02). A cross-tab `storage`-event listener that calls `signOut` would close it. Real and trivially reachable (open two tabs), but outside the frozen intent, which scopes logout to the acting session, and it is a pre-existing architectural gap the expiry path shares — a focused cross-tab-sync change, not this feature's.
